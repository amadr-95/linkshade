# Only for local development
# run: docker compose up -d --build
services:
  linkshade-db:
    container_name: linkshade-db
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: linkshade-db
      PGDATA: /data/postgres
    volumes:
      - linkshade-db:/data/postgres
    ports:
      - "5432:5432"
    networks:
      - linkshade-network
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d linkshade-db" ]
      interval: 5s
      timeout: 3s
      retries: 3

  linkshade-app:
    env_file:
      - .env.local
    container_name: linkshade-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      # The URL need to point to the container name, not localhost
      SPRING_DATASOURCE_URL: jdbc:postgresql://linkshade-db:5432/linkshade-db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
    depends_on:
      linkshade-db:
        condition: service_healthy
    networks:
      - linkshade-network
    restart: unless-stopped

networks:
  linkshade-network:
    driver: bridge

volumes:
  linkshade-db: