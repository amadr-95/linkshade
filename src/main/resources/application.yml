spring:
  application:
    name: url-shortener
# Database config
  datasource:
    url: jdbc:postgresql://localhost:5432/url-shortener-db
    username: asabido
    password: asabido
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    open-in-view: false
  # to load scripts under resources folder
#  sql:
#    init:
#      mode: always

